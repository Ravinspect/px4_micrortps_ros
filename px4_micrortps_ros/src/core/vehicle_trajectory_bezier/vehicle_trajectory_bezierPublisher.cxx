// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file vehicle_trajectory_bezierPublisher.cpp
 * This file contains the implementation of the publisher functions.
 *
 * This file was generated by the tool fastcdrgen.
 */


#include <fastrtps/participant/Participant.h>
#include <fastrtps/attributes/ParticipantAttributes.h>
#include <fastrtps/publisher/Publisher.h>
#include <fastrtps/attributes/PublisherAttributes.h>

#include <fastrtps/Domain.h>

#include <thread>
#include <chrono>

#include "vehicle_trajectory_bezierPublisher.h"

using namespace eprosima::fastrtps;
using namespace eprosima::fastrtps::rtps;

vehicle_trajectory_bezierPublisher::vehicle_trajectory_bezierPublisher() : mp_participant(nullptr), mp_publisher(nullptr) {}

vehicle_trajectory_bezierPublisher::~vehicle_trajectory_bezierPublisher() {	Domain::removeParticipant(mp_participant);}

bool vehicle_trajectory_bezierPublisher::init()
{
    // Create RTPSParticipant

    ParticipantAttributes PParam;
    PParam.rtps.setName("Participant_publisher");  //You can put here the name you want
    mp_participant = Domain::createParticipant(PParam);
    if(mp_participant == nullptr)
    {
        return false;
    }

    //Register the type

    Domain::registerType(mp_participant, static_cast<TopicDataType*>(&myType));

    // Create Publisher

    PublisherAttributes Wparam;
    Wparam.topic.topicKind = NO_KEY;
    Wparam.topic.topicDataType = myType.getName();  //This type MUST be registered
    Wparam.topic.topicName = "vehicle_trajectory_bezierPubSubTopic";

    mp_publisher = Domain::createPublisher(mp_participant,Wparam,static_cast<PublisherListener*>(&m_listener));

    if(mp_publisher == nullptr)
    {
        return false;
    }

    std::cout << "Publisher created, waiting for Subscribers." << std::endl;
    return true;
}

void vehicle_trajectory_bezierPublisher::PubListener::onPublicationMatched(Publisher* pub,MatchingInfo& info)
{
    (void)pub;

    if (info.status == MATCHED_MATCHING)
    {
        n_matched++;
        std::cout << "Publisher matched" << std::endl;
    }
    else
    {
        n_matched--;
        std::cout << "Publisher unmatched" << std::endl;
    }
}

void vehicle_trajectory_bezierPublisher::run(const px4_msgs::VehicleTrajectoryBezier& input)
{
    vehicle_trajectory_bezier st;


    st.timestamp_(input.timestamp);             

    std::array<trajectory_bezier, 5> control_points;
    for (int i = 0; i < 5; i++)
    {
        px4_msgs::TrajectoryBezier tb_msgs = input.control_points[i];
        
        trajectory_bezier tb;
        tb.timestamp_(tb_msgs.timestamp);

        std::array<float, 3> position = { tb_msgs.position[0], tb_msgs.position[1], tb_msgs.position[2] };
        tb.position(position);
        tb.yaw_(tb_msgs.yaw);
        tb.delta_(tb_msgs.delta);

        control_points[i] = tb;
    }

    st.control_points(control_points);

    st.bezier_order_(input.bezier_order);

    mp_publisher->write(&st);
}
